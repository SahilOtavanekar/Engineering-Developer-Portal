---
# https://backstage.io/docs/features/software-catalog/descriptor-format#kind-user
apiVersion: backstage.io/v1alpha1
kind: User
metadata:
  name: guest
spec:
  memberOf: [guests]
---
# https://backstage.io/docs/features/software-catalog/descriptor-format#kind-group
apiVersion: backstage.io/v1alpha1
kind: Group
metadata:
  name: guests
spec:
  type: team
  children: []
---
# Your GitHub user. The name must match your GitHub login (case-insensitive),
# because app-config.yaml uses the `usernameMatchingUserEntityName` resolver.
apiVersion: backstage.io/v1alpha1
kind: User
metadata:
  name: sahilotavanekar
spec:
  profile:
    displayName: Sahil Otavanekar
    email: sahil.otavanekar@demandai.co
  memberOf: [platform]
---
apiVersion: backstage.io/v1alpha1
kind: Group
metadata:
  name: platform
  description: Platform engineering team
spec:
  type: team
  children: []
---
# Placeholder owner for repositories ingested from Bitbucket before real
# ownership data exists. Repositories owned by this group are the backlog the
# ownership campaign works through -- they must stay visible and countable.
apiVersion: backstage.io/v1alpha1
kind: Group
metadata:
  name: unowned
  description: Repositories with no identified owner yet
spec:
  type: team
  children: []
